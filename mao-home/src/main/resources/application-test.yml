server:
  port: 10001

spring:
  application:
    name: mao-home

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/
    # 是否从其他服务中心同步服务列表
    fetch-registry: false
    # 是否把自己作为服务注册到其他服务中心
    register-with-eureka: false